[pytest]
testpaths = project
python_files = test_*.py
addopts =
    -p no:cacheprovider
    -s
    --create-db
    --cov-report term
    --cov-report html:cov_report
    --cov=project
DJANGO_SETTINGS_MODULE = project.settings
