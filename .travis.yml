language: python
python:
  - 3.6
services:
  - postgresql
install:
  - pip install -r requirements/travis.txt
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
script:
  - fab coverage:project,report
after_success:
  - coveralls
